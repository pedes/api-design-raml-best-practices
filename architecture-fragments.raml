#%RAML 1.0 Library
usage: This library should be used to REST APIs so it follows best practices in terms of caching, pagination, security/authorization.

traits:
  Cacheable: !include Cacheable.raml
  Pagination: !include Pagination.raml

securitySchemes:
  OAuth2: !include OAuth2.raml